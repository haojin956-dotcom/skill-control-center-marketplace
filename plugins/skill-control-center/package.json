{
  "name": "skill-control-center",
  "version": "0.2.3",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "configure-runtime": "node scripts/configure-runtime.mjs",
    "check": "node --check scripts/server.mjs && node --check scripts/state-store.mjs && node --check scripts/hook-handler.mjs && node --check scripts/configure-runtime.mjs && node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
